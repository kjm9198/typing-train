const config = {
  plugins: {
    ["@tailwindcss/postcss"]: {}, // Tailwind CSS plugin with default options
    autoprefixer: {},             // Autoprefixer plugin with default options
  },
};

export default config;